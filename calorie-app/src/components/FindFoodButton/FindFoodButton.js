/* eslint-disable react/prop-types */
import { Button } from '@mui/material';

function FindFoodButton() {
  const handleClick = () => {
  };
  return (
    <Button variant="outlined" onClick={handleClick}>Search</Button>
  );
}

export default FindFoodButton;
